---
phase: 03-extended-city-data-tools
plan: 03
type: execute
wave: 3
depends_on: [03-01, 03-02]
files_modified: []
autonomous: false
requirements: [DATA-01, DATA-02, DATA-03, DATA-04, DATA-05]

must_haves:
  truths:
    - "Claude returns specific budget numbers (not placeholder or error) when asked about city finances"
    - "Claude returns traffic flow or congestion data when asked about traffic"
    - "Claude returns electricity/water/health data when asked about city services"
    - "Disabled tools do not appear in the API request tools array"
  artifacts: []
  key_links: []
---

<objective>
Verify the full Phase 3 delivery in-game: build, deploy, load a city, and confirm Claude uses the new tools with real ECS data.

Purpose: End-to-end validation that all three new tools return real ECS numbers and tool toggles work correctly (per D-23).
Output: Confirmed working or issue list for gap closure.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-extended-city-data-tools/03-CONTEXT.md
@.planning/phases/03-extended-city-data-tools/03-01-SUMMARY.md
@.planning/phases/03-extended-city-data-tools/03-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build and deploy mod</name>
  <files></files>
  <read_first>
    - src/Systems/CityDataSystem.cs (verify it has budget/traffic/services properties)
    - src/Systems/ClaudeAPISystem.cs (verify 3 new tools registered)
  </read_first>
  <action>
Run the full build to confirm everything compiles cleanly before in-game testing:
1. `cd src && dotnet build -c Release` -- must exit 0 with no warnings about the new code
2. `cd UI && npm run build` -- must exit 0 (no UI changes in Phase 3, but confirm nothing is broken)
3. Verify the output DLL exists at `%AppData%/../LocalLow/Colossal Order/Cities Skylines II/Mods/CityAgent/`
  </action>
  <verify>
    <automated>cd src && dotnet build -c Release 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `dotnet build -c Release` exits 0
    - CityAgent.dll exists in the mod output folder
  </acceptance_criteria>
  <done>Mod builds and deploys successfully.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: In-game verification of new tools and toggles</name>
  <files></files>
  <action>
Human verifies all Phase 3 deliverables in-game. This is a manual verification checkpoint per D-23.
  </action>
  <verify>Human confirms all 9 verification steps pass.</verify>
  <done>All three tools return real ECS data; toggles filter correctly; settings UI shows Data Tools section.</done>
  <what-built>Three new ECS data tools (get_budget, get_traffic_summary, get_services_summary) with per-tool toggles in mod settings and updated system prompt.</what-built>
  <how-to-verify>
    1. Launch Cities: Skylines 2 and load a city with roads, zones, and services
    2. Open the CityAgent panel
    3. Ask: "What are my city finances?" -- verify Claude calls get_budget and response includes specific numbers (balance, tax income, expenses), not "unavailable" or error text
    4. Ask: "How is traffic in my city?" -- verify Claude calls get_traffic_summary and response references flow score or bottleneck count
    5. Ask: "How are my city services doing?" -- verify Claude calls get_services_summary and response includes electricity production/consumption, water stats, or sick citizen count
    6. Open mod settings (Options > CityAgent) -- verify "Data Tools" section appears as the 4th group
    7. Verify 7 toggles are visible with human-readable labels (Population, Building Demand, Workforce, Zoning Summary, City Finances, Traffic, City Services)
    8. Disable "City Finances" toggle, send a new message asking about finances -- verify Claude does NOT call get_budget (it won't be in the tools array)
    9. Re-enable "City Finances" toggle -- verify it works again on the next message without restart
  </how-to-verify>
  <resume-signal>Type "approved" if all verifications pass, or describe which checks failed.</resume-signal>
</task>

</tasks>

<verification>
- Build succeeds
- All three tools return real ECS data in-game
- Tool toggles filter API requests correctly
- Settings UI shows Data Tools section with human-readable labels
</verification>

<success_criteria>
- Player asks about finances, traffic, and services -- Claude responds with specific numbers from ECS
- Disabling a tool in settings removes it from the next API call
- No errors in mod log related to new tools
</success_criteria>

<output>
After completion, create `.planning/phases/03-extended-city-data-tools/03-03-SUMMARY.md`
</output>
